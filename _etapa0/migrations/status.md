# Registro de aplicação de migrations

| Migration | Desenvolvimento | Produção |
|---|---|---|
| 0000_baseline.sql | ✅ Aplicada em 31/07/2026, projeto `rg-gastro-DEV` — validada (13 tabelas, 3 funções, 1 trigger, RLS + 16 políticas, 0 usuários, 0 dados) | Não aplicada (produção já tem o schema equivalente aplicado manualmente antes deste projeto de migrations existir — ver nota) |
| 0001_v4_cursos_processos_documentos_timeline.sql | ✅ Aplicada em 31/07/2026, projeto `rg-gastro-DEV` — validada (4 tabelas, 1 coluna nova, 2 FKs cascade, RLS + política nas 4) | Não aplicada (idem) |
| 0002_v5_categorias_vendas_equipe_visitas_links_cursos.sql | ✅ Aplicada em 31/07/2026, projeto `rg-gastro-DEV` — validada (6 tabelas, 14 colunas novas, 1 FK cascade, 1 unique, RLS + políticas nas 6; total 23 tabelas no schema) | Não aplicada (idem) |
| 0003_v6_lotes_importacao_hotmart.sql (Etapa 1B.1) | ✅ Aplicada em 01/08/2026, projeto `rg-gastro-DEV` — executada via `../0003_execucao_e_validacao_supabase_DEV.sql` (migration + 9 validações + 4 testes de constraint, todos em blocos `DO $$...END $$` isolados); execução sem erro ("Success. No rows returned."). 2 tabelas novas (`rg_vendas_lotes`, `rg_vendas_alteracoes_historico`), 7 colunas novas em `rg_vendas`, 1 coluna nova em `rg_financeiro`, 10 índices, RLS + política admin-only nas 2 tabelas novas. Testes de constraint (2 válidos, 2 inválidos) comportaram-se como esperado, sem dado de teste persistido. Nenhuma alteração funcional em `app.html` acompanha esta migration (só schema) | Não aplicada — decisão própria, não experimental: nenhuma migration roda em produção sem autorização explícita separada |
| 0004_v7_trava_importacao_parcial.sql (Etapa 1B.2 — preparação de schema) | ✅ Aplicada em 01/08/2026, projeto `rg-gastro-DEV` — executada via `../0004_execucao_e_validacao_supabase_DEV.sql` (migration + 6 validações + 2 testes de constraint, todos em blocos `DO $$...END $$` isolados); execução sem erro ("Success. No rows returned."). Constraint de `rg_vendas_lotes.status` ampliada para `processando`/`ativo`/`erro`/`desfeito`; colunas novas `erro_linha`/`erro_detalhe` em `rg_vendas_lotes`; coluna nova `importacao_concluida boolean not null default true` em `rg_vendas`; 1 índice parcial (`where importacao_concluida = false`). Teste dos 4 status válidos aceito; teste de status inválido bloqueado pela constraint; nenhum dado de teste persistido. Nenhuma alteração funcional em `app.html` acompanha esta migration (só schema) | Não aplicada — mesma decisão registrada acima |
| 0005_v8_rpc_reprocessamento_transacional.sql (Etapa 1B.3 — RPC de reprocessamento) | ✅ Aplicada e validada em 02/08/2026, projeto `rg-gastro-DEV` — executada via `../0005_execucao_e_validacao_supabase_DEV.sql` (função `public.rg_reprocessar_lote`, primeira RPC de negócio do projeto, `security invoker` + `search_path=''`, com 13 cenários de teste — 0, A–M — em bloco `DO $$...END $$` isolado, tudo-ou-nada). Primeira execução falhou (`ERROR: P0001: somente administradores podem reprocessar importações`) por ausência de contexto `auth.uid()` no SQL Editor, que conecta direto ao Postgres fora do PostgREST e nunca preenche `request.jwt.claims`/`request.jwt.claim.sub`; transação revertida integralmente (confirmado por consulta de leitura: função, fixtures e histórico de teste ausentes). Script corrigido para localizar dinamicamente um admin real e aprovado em `public.profiles` e simular esse contexto de autenticação apenas dentro da transação via `set_config(..., true)` (reverte sozinho no COMMIT/ROLLBACK). Segunda execução revelou ainda que `REVOKE EXECUTE ... FROM PUBLIC` sozinho não removia o `EXECUTE` que o Supabase concede por padrão a `anon`/`service_role` via `ALTER DEFAULT PRIVILEGES` do projeto; corrigido nomeando `public, anon, service_role` explicitamente no `REVOKE` (migration e script), com um teste de catálogo novo (TESTE M, via `has_function_privilege()`/`aclexplode()`, não leitura manual de ACL) garantindo isso na própria transação. Execução final: "Success. No rows returned." — todos os testes aprovados, nenhuma fixture residual. Auditoria final por consulta de leitura confirmou: `security invoker`; `search_path=''`; `EXECUTE` — `PUBLIC`=false, `anon`=false, `authenticated`=true, `service_role`=false; assinatura conforme o contrato aprovado. Nenhuma alteração funcional em `app.html` acompanha esta migration (só a função e as permissões) | Não aplicada — mesma decisão registrada acima |
| 0006_v10_pendencia_financeira_reprocessamento.sql (Etapa 1B.5 — pendência financeira no reprocessamento) | ✅ Aplicada e validada em 02/08/2026, projeto `rg-gastro-DEV` — executada via `../0006_execucao_e_validacao_supabase_DEV.sql` (estende `public.rg_reprocessar_lote` da 0005, mesma assinatura/segurança; 2 colunas novas aditivas e nullable em `rg_vendas` — `pendencia_financeira_resolvida_por uuid` FK `profiles(user_id)` on delete set null, `pendencia_financeira_resolvida_em timestamptz` — auditoria de quem/quando resolveu; usa `pendencia_financeira`/`rg_vendas_lotes.pendencias_financeiras`, ambas já existentes desde a 0003 e sem uso até aqui). Regra: uma venda com `fin_lancado=true` que sofre alteração real de status, valor ou líquido no reprocessamento tem `pendencia_financeira` marcada `true` e `resolvida_por`/`resolvida_em` limpos (reabre qualquer resolução anterior); o contador `pendencias_financeiras` só soma quando a venda ainda não estava pendente; venda não lançada ou sem alteração real nunca gera pendência; a função nunca insere, atualiza ou de qualquer forma toca `rg_financeiro` — nenhum ajuste automático, correção sempre manual. 21 cenários de teste em bloco `DO $$...END $$` isolado, tudo-ou-nada, reaproveitando a técnica de `set_config` com admin real do DEV já validada na 0005: TESTE M (permissões, regressão), TESTE 0 (formato canônico, regressão), TESTES A–L (fluxo de reprocessamento, regressão), TESTE N (pendência nova via status), TESTE O (pendência já aberta via valor, não recontada), TESTE P (pendência via líquido, limpa resolução anterior), TESTE Q (venda não lançada nunca vira pendência), TESTE R (venda lançada sem alteração real não vira pendência), TESTE T (rollback integral desfaz também a pendência), TESTE S (hash do conteúdo de `rg_financeiro` idêntico antes/depois de todos os testes — confirma que a tabela nunca foi tocada). Autoauditoria prévia à execução identificou e corrigiu 2 lacunas nos testes (TESTE O originalmente disparava por status, não por valor isoladamente; faltava um teste de rollback específico para a pendência — virou o TESTE T) antes de qualquer execução real. Execução final: "Success. No rows returned." — todos os testes aprovados, nenhuma fixture residual. Nenhuma alteração funcional em `app.html` acompanha esta migration (só a função e as colunas de auditoria) | Não aplicada — mesma decisão registrada acima |

**Nota sobre produção:** o banco de produção já existe e presumivelmente já reflete o conteúdo equivalente a estas três migrations (aplicado originalmente como um único `setup-supabase.sql` colado no SQL Editor, antes deste sistema de versionamento existir). Estas migrations não devem ser rodadas contra produção — servem como baseline para o Supabase de **desenvolvimento**, e como ponto de partida versionado para qualquer alteração de schema **futura**, que a partir de agora deve vir como uma nova migration numerada (`0003_...sql` e assim por diante), nunca como SQL solto.

**Schema do Supabase DEV concluído em 31/07/2026** — as 3 migrations rodaram na ordem correta, cada uma dentro de sua própria transação, todas validadas por consultas somente-leitura antes de avançar para a próxima. Nenhum usuário foi criado, nenhum dado real foi inserido. O schema do projeto `rg-gastro-DEV` agora espelha o schema descrito no backup (ver `../01-inventario-schema-backup.md`).

Este arquivo deve ser atualizado manualmente toda vez que uma migration for de fato aplicada em algum ambiente.
